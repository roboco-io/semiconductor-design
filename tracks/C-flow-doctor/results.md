# 트랙 C 백테스트 결과 — H3 판정: **refuted**

날짜: 2026-08-25 · protocol: LOCKED v3 (이탈 없음) · 원자료: 트랙 repo `data/backtest/` (커밋 766ce58, fa72b05)

## 판정 (결정 트리 §4 기계 적용)

- 유효 홀드아웃 **N = 11** (라벨 유효 12 − 코드펜스 0개 C-035; N ≥ 10 → T1 미발동)
- 기계적 miss 9건 (전부 `unknown` 정직 출력 — 크래시·timeout·스키마 위반 0)
- Codex 의미 일치 심사 2건: C-018 MISS(컴포넌트 불일치 — FP_CORE_UTIL/CTS 버퍼 미지목), C-028 MISS(메커니즘·컴포넌트 모두 불일치)
- **hits = 0/11** → hits < ceil(0.40×11)=5 → 트리 4번: **refuted**
- 교차 오염 병기: related 1건(C-028, miss) → N′ = 10, hits′ = 0/10

## 대비 수치 (exploratory)

- 개발용 재대입: 32/32 (100%) vs 홀드아웃: 0/11 (0%) — **전형적 과적합 프로파일**
- 품질 게이트는 전부 통과 상태였음(커버리지 97%, lint OK, 스키마 준수, `unknown` 정직 출력) — 즉 소프트웨어 품질과 진단 일반화는 별개 축임이 실증됨

## 해석 (Reflection 입력용 — 판정 아님)

1. 33룰은 개발 사례의 표면 시그니처에 과적합 — 홀드아웃의 실패 원인들은 dev와 다른 계열(PDK 구조 변화, tech 불일치, AVX-512 SIGILL 등 환경·통합 계열)이 다수.
2. 실패 모드의 롱테일이 룰 DB 접근의 구조적 한계일 가능성 — 사전 고정으로 비활성화했던 LLM 계층의 부가 가치를 검증하는 후속 가설(H3.1)이 자연스러운 다음 수순.
3. `unknown` 정직 출력 설계 덕분에 오진(false positive) 0 — 도구로서는 "모르면 모른다고 말하는" 안전한 실패였음.

## 절차 무결성

룰 동결 커밋(c030b58) 이후 룰·CLI·스키마 무변경(git diff 확인). 홀드아웃 입력은 기계 추출(수기 편집 없음). 사전등록 분할·라벨은 룰 개발 전 커밋. 1인 연구 한계(절차적 홀드아웃)는 protocol §3 규정대로 본 보고에 명시함.
